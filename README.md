simple project templating system, usage:
```bash
template $(template name) $(project name)
```
where template name is the template inside your filesystem in the ~/simple_templates/ directory and project name is the output directory this simple script will generate your new project into (relative)
